name: Shell scripts

on:
  push:
    branches:
      - main
      - master
    paths:
      - "install"
      - "setup"
      - "profile/.bashrc_append"
      - "profile/gwt"
      - "profile/bashrc.d/**"
      - "profile/*.sh"
      - "utils/*.sh"
      - "tmuxifiers/*.sh"
      - "tests/**"
      - "tests/check-function-syntax.sh"
      - "tests/run-ci-checks.sh"
      - ".github/workflows/scripts.yml"
  pull_request:
    branches:
      - main
      - master
    paths:
      - "install"
      - "setup"
      - "profile/.bashrc_append"
      - "profile/gwt"
      - "profile/bashrc.d/**"
      - "profile/*.sh"
      - "utils/*.sh"
      - "tmuxifiers/*.sh"
      - "tests/**"
      - "tests/check-function-syntax.sh"
      - "tests/run-ci-checks.sh"
      - ".github/workflows/scripts.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  syntax:
    name: Bash syntax
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: bash -n
        run: |
          files=(
            install
            setup
            profile/.bashrc_append
            profile/gwt
            profile/bashrc.d/*.bash
            profile/*.sh
            utils/*.sh
            tmuxifiers/*.sh
            tests/*.sh
            tests/lib/*.sh
          )
          bash -n "${files[@]}"

  function-syntax:
    name: Function syntax
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check function syntax convention
        run: bash tests/check-function-syntax.sh

  smoke:
    name: Smoke tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run smoke tests
        run: bash tests/bashrc-smoke.sh
      - name: Run gwt tests
        run: bash tests/gwt-test.sh
      - name: Run pi wrapper tests
        run: bash tests/pi-unleashed-safely-test.sh

  tmux-equalize-row:
    name: tmux-equalize-row tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run tmux-equalize-row tests
        run: bash tests/tmux-equalize-row-test.sh

  shellcheck:
    name: ShellCheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install shellcheck
        run: sudo apt-get update && sudo apt-get install -y shellcheck
      - name: Lint shell files
        run: |
          # Refactored files only; expand scope as install/setup/utils are cleaned up
          files=(
            profile/.bashrc_append
            profile/gwt
            profile/bashrc.d/*.bash
            profile/pi-unleashed-safely.sh
            tests/*.sh
            tests/lib/*.sh
          )
          shellcheck -x "${files[@]}"

  shfmt:
    name: shfmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install shfmt
        run: |
          SHFMT_VERSION="v3.10.0"
          curl -sS -L "https://github.com/mvdan/sh/releases/download/${SHFMT_VERSION}/shfmt_${SHFMT_VERSION}_linux_amd64" -o /usr/local/bin/shfmt
          chmod +x /usr/local/bin/shfmt
      - name: Check formatting
        run: |
          # Refactored files only; expand scope as install/setup/utils are cleaned up
          files=(
            profile/.bashrc_append
            profile/gwt
            profile/bashrc.d/*.bash
            profile/pi-unleashed-safely.sh
            tests/*.sh
            tests/lib/*.sh
          )
          shfmt -i 2 -d "${files[@]}"
